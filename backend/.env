# Backend environment
# Express server port
PORT=3001

# JWT signing secret for mock auth (change in production)
JWT_SECRET=dev-secret-change-me

# Logging level for Winston (error|warn|info|http|verbose|debug|silly)
LOG_LEVEL=info